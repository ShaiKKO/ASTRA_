// SPDX-License-Identifier: MIT OR Apache-2.0
//! ASTRA_ Gateway — Model Gateway (BYOM).
//!
//! Unified interface to LLM providers: OpenAI, Anthropic, LocalRuntime, CustomHTTP.
//!
//! # Performance
//! Gateway overhead: ≤5ms p95 (excluding provider latency).

#[cfg(test)]
mod tests {
    #[test]
    fn crate_compiles() {
        // Stub test - implementation pending
    }
}
